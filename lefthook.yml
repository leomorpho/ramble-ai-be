pre-commit:
  parallel: true
  commands:
    backend-tests:
      glob: "pb/**/*.go"
      run: cd pb && go test ./... -v
      fail_text: "Backend Go tests failed"
    
    frontend-tests:
      glob: "sk/**/*.{js,ts,svelte}"
      run: cd sk && npm run test
      fail_text: "Frontend tests failed"
